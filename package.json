{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "example": "pulp test -r cat > html/index.js"
  },
  "dependencies": {
    "create-react-class": "^15.6.1",
    "react": "^16.0.2",
    "react-dom": "^16.0.2"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.7",
    "rimraf": "^2.6.2"
  }
}
